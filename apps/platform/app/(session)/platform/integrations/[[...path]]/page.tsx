/**
 * Marker page for the integration management routes. The UI lives in the layout so
 * it persists across selection changes (see layout.tsx); this page exists only to
 * make the optional catch-all route — and every selection path under it, e.g.
 * /platform/integrations/i/<id> — resolvable.
 */
export default function IntegrationsPage() {
  return null;
}
